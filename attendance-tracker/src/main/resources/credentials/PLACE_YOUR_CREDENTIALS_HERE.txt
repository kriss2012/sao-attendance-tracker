Put your Google Cloud service-account key here, named exactly:

    google-credentials.json

Steps to generate it are in the main README.md under
"Connecting your Google Sheet". Until this file is replaced with a
real key, any Sheets sync request will fail with a clear
GoogleSheetSyncException instead of crashing the app - everything
else (dashboard, REST API, demo data) works normally without it.
